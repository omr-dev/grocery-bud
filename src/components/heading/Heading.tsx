import styles from './heading.module.css';

const Heading = () => {
    return (
        <div>
            <h1 className={styles.title}>grocery bud</h1>
        </div>
    );
};

export default Heading;
